# shared-contracts

Shared event contracts used by `order-service` and `notification-service` inside the monorepo.

See [../README.md](../README.md) for the full system overview and build instructions.
